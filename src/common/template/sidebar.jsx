import React from "react";
import Menu from "./menu";

export default (prrps) => (
  <aside className="main-sidebar">
    <section className="sidebar">
      <Menu></Menu>
    </section>
  </aside>
);
